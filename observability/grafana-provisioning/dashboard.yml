apiVersion: 1

providers:
  - name: 'golden-signals'  
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    options:
      # The path where Grafana will look for dashboard JSON files
      path: /etc/grafana/provisioning/dashboards